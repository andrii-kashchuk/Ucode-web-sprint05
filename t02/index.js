/*
  Task 02
  Task name: Range
*/

function range(a, b) {
  let a = Number;
  let b = Number;
  a = prompt("Enter range from 1 to 60. Please, enter start point:");
  b = prompt("Enter the end of range:");
  if (a < 1 || a > 60 || b < 0 || b > 60 || a < b) {
      alert("Wrong Input");
  } else if {
    
  }
}